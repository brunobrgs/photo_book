<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= Settings.title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="author" content="Bruno Borges">

  <!-- Styles -->
  <link href="http://fonts.googleapis.com/css?family=Pacifico" rel="stylesheet" type="text/css">
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="masthead clearfix">
    <div class="container">
      <div class="logo pull-left"><%= link_to Settings.logo, root_path %></div>
      <div class="socials pull-right">
        <% if Settings.twitter %>
          <a href="<%= Settings.twitter %>"><i class="icon-twitter"></i></a>
        <% end %>
        <% if Settings.facebook %>
          <a href="<%= Settings.facebook %>" target="_blank"><i class="icon-facebook"></i></a>
        <% end %>
        <% if Settings.linkedin %>
          <a href="<%= Settings.linkedin %>"><i class="icon-linkedin"></i></a>
        <% end %>
        <% if Settings.pinterest %>
          <a href="<%= Settings.pinterest %>"><i class="icon-pinterest"></i></a>
        <% end %>
        <% if Settings.email %>
          <a href="<%= new_contact_path %>"><i class="icon-envelope"></i></a>
        <% end %>
      </div>
    </div>
  </div>

  <div class="container">
    <%= bootstrap_flash %>
    <%= yield %>
  </div>
</body>
</html>
